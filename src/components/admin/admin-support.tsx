import { updateSupportStatus } from "@/features/customer/admin-support-actions";
import type { AdminSupportData } from "@/features/customer/admin-support";

export function AdminSupport({ data, result }: { data: AdminSupportData; result?: string }) {
  return <main id="main-content" className="px-5 py-8 sm:px-8"><div className="mx-auto max-w-6xl"><div className="border-b border-white/10 pb-7"><p className="text-xs font-semibold uppercase tracking-[0.1em] text-muted">Customer operations</p><h1 className="mt-3 text-3xl font-semibold tracking-[-0.045em]">Order support</h1><p className="mt-2 max-w-2xl leading-6 text-secondary">Review private customer messages and maintain a durable request status. Never copy credentials or delivery tokens into notes.</p></div>{result && <p className="mt-6 rounded-xl border border-white/10 p-4 text-sm text-secondary">{result === "updated" ? "Support status updated." : "Support status could not be updated."}</p>}{data.status !== "ready" ? <div className="mt-7 rounded-xl border border-dashed border-white/15 p-6 text-secondary">{data.status === "unconfigured" ? "Connect Supabase and apply the Phase 8 migration to review support." : "Support records could not be verified."}</div> : data.requests.length === 0 ? <div className="mt-7 rounded-xl border border-dashed border-white/15 p-6 text-secondary">No customer support requests exist.</div> : <div className="mt-7 grid gap-4">{data.requests.map((request) => <article key={request.id} className="rounded-xl border border-white/10 bg-surface p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><p className="text-xs text-muted">{request.order.order_number} · {request.order.customer_email}</p><h2 className="mt-2 text-lg font-semibold">{request.subject}</h2></div><span className="w-fit rounded-full border border-white/15 px-3 py-1 text-xs font-semibold capitalize">{request.status.replace("_", " ")}</span></div><p className="mt-5 whitespace-pre-wrap border-y border-white/10 py-5 text-sm leading-7 text-secondary">{request.message}</p><form action={updateSupportStatus.bind(null, request.id)} className="mt-4 flex flex-wrap items-end gap-3"><label className="grid gap-2 text-xs font-semibold text-muted">Status<select name="status" defaultValue={request.status} className="min-h-10 rounded-lg border border-white/15 bg-background px-3 text-sm text-foreground"><option value="open">Open</option><option value="in_progress">In progress</option><option value="resolved">Resolved</option><option value="closed">Closed</option></select></label><button className="min-h-10 rounded-lg border border-white/15 px-4 text-xs font-semibold">Save status</button></form></article>)}</div>}</div></main>;
}
